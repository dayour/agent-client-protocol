name: Release-plz

on:
  push:
    branches:
      - main

jobs:
  fork-release-disabled:
    name: Fork release posture
    runs-on: ubuntu-latest
    if: ${{ github.repository != 'agentclientprotocol/agent-client-protocol' }}
    steps:
      - name: Explain fork release posture
        run: |
          echo "release-plz automation is disabled in forks."
          echo "This fork tracks upstream ACP protocol content but is independently maintained for hardening work."
          echo "Do not publish crates.io or npm releases under upstream names from ${GITHUB_REPOSITORY}."

  release-plz-release:
    name: Release-plz release
    environment: release # Optional: for enhanced security
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'agentclientprotocol/agent-client-protocol' }}
    permissions:
      contents: write
      id-token: write
    steps:
      - &checkout
        name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0
          persist-credentials: false
      - &install-rust
        name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: stable
      - &generate-token
        # Generating a GitHub token, so that PRs and tags created by
        # the release-plz-action can trigger actions workflows.
        name: Generate GitHub token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        id: generate-token
        with:
          # GitHub App ID secret name
          app-id: ${{ secrets.RELEASE_PLZ_APP_ID }}
          # GitHub App private key secret name
          private-key: ${{ secrets.RELEASE_PLZ_APP_PRIVATE_KEY }}
      - name: Run release-plz
        uses: release-plz/action@2eb1d8bcb770b4c48ccfaad919734b38b51958c9
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}

  release-plz-pr:
    name: Release-plz PR
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'agentclientprotocol/agent-client-protocol' }}
    permissions:
      pull-requests: write
      contents: write
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - *checkout
      - *install-rust
      - *generate-token
      - name: Run release-plz
        uses: release-plz/action@2eb1d8bcb770b4c48ccfaad919734b38b51958c9
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
